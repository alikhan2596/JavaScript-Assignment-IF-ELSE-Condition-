var maxAge = prompt ("Enter Your Maximum Age!");
var currentAge = prompt ("Enter Your Current Age!");

if (currentAge < maxAge || currentAge == maxAge) {
    alert ('"You are Welcome!"')
}

else {
    alert ("Invalid Age!");
}