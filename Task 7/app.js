var currentFeul = prompt ("Please enter the remaining feul in car (in litres)!");

if (currentFeul < "0.24litres") {
    alert ("Please refill the feul in your car!");
}

else {
    alert ("Your feul is enough!");
}